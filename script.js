let greet = "Welcome2thedungeon"

console.log(greet("some name"));

function getCircumference(diameter) { const pi = Math.PI; }
// Pi constant return pi * diameter;

function getCircumference(diameter) {
    const pi = Math.PI;
     // Pi constant
    return pi * diameter;
     // Circumference formula: π * diameter
}
